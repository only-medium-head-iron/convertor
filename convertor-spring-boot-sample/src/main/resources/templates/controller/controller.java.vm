package ${controllerPackage};

import ${requestPackage}.${entityName}CreateReq;
import ${requestPackage}.${entityName}UpdateReq;
import ${requestPackage}.${entityName}PageQuery;
import ${responsePackage}.${entityName}RespVO;
import ${servicePackage}.${entityName}Service;
import com.wt.wbp.tms.common.CommonResult;
import com.mybatisflex.core.paginate.Page;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;

import javax.annotation.Resource;

/**
 * ${comment} 控制器
 */
@RestController
@RequestMapping("/api/${module}/${entityVariable}")
@Tag(name = "${comment}管理")
public class ${entityName}Controller {

    @Resource
    private ${entityName}Service ${entityVariable}Service;

    @PostMapping("/create")
    @Operation(summary = "创建${comment}")
    public CommonResult<Long> create(@Validated @RequestBody ${entityName}CreateReq req) {
        Long id = ${entityVariable}Service.create(req);
        return CommonResult.success(id, "创建${comment}成功");
    }

    @PutMapping("/update")
    @Operation(summary = "更新${comment}")
    public CommonResult<Boolean> update(@Validated @RequestBody ${entityName}UpdateReq req) {
        Boolean result = ${entityVariable}Service.update(req);
        return CommonResult.success(result, result ? "更新${comment}成功" : "更新${comment}失败");
    }

    @DeleteMapping("/delete/{id}")
    @Operation(summary = "删除${comment}")
    public CommonResult<Boolean> delete(@PathVariable Long id) {
        Boolean result = ${entityVariable}Service.delete(id);
        return CommonResult.success(result, result ? "删除${comment}成功" : "删除${comment}失败");
    }

    @PostMapping("/page-query")
    @Operation(summary = "分页查询${comment}")
    public CommonResult<Page<${entityName}RespVO>> pageQuery(@Validated @RequestBody ${entityName}PageQuery query) {
        Page<${entityName}RespVO> result = ${entityVariable}Service.pageQuery(query);
        return CommonResult.success(result, "查询${comment}成功");
    }

    @GetMapping("/get/{id}")
    @Operation(summary = "获取${comment}详情")
    public CommonResult<${entityName}RespVO> getById(@PathVariable Long id) {
        ${entityName}RespVO result = ${entityVariable}Service.getById(id);
        return CommonResult.success(result, result != null ? "获取${comment}成功" : "${comment}不存在");
    }
}